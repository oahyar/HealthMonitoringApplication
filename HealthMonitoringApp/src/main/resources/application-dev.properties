# ?? API-monitoring properties ??????????????????????????????????????????????

# ?? Tell Hibernate to create or update tables based on your @Entity classes
spring.jpa.hibernate.ddl-auto=update

# how often (ms) to run all checks
monitor.rateMillis=30000

# define each endpoint as monitor.endpoints.<key>=<url>
monitor.endpoints.users-service=http://localhost:8089/users/health
monitor.endpoints.orders-service=http://localhost:8089/orders/health
monitor.endpoints.unknown-service=http://localhost:8089/unknown/health
monitor.endpoints.slow-service=http://localhost:8089/slow/health
monitor.endpoints.flaky-service=http://localhost:8089/flaky/health